export declare class Bag {
	constructor();

	findModel(element: HTMLElement): any;

	hasModel(element: HTMLElement): boolean;

	addRow(row: any) ;

	addCell(cell: any);

	deleteRow(row: any);

	deleteCell(cell: any);
}