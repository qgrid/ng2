export declare class Keyboard {
	static isPrintable(code: string): boolean;
	static isControl(code: string): boolean;
	static stringify(code: string): string;
	static translate(code: string): string;
}
