import { Command } from '../command/command';
import { GridPlugin } from '../plugin/grid.plugin';

export declare class FocusInvalidateCommand extends Command<any> {
    constructor(plugin: GridPlugin);
}
